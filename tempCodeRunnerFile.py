 # tmp, r, s = lts.local_search_tabou(starting_time,tmp,
            #                         ring_size,input_file_name,30)
            # if tmp < best_L :
            #     best_L = tmp
            #     best_ring = r
            #     best_star = s